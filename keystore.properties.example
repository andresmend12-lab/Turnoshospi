# =============================================================================
# KEYSTORE CONFIGURATION FOR RELEASE BUILDS
# =============================================================================
#
# Instructions:
# 1. Copy this file to 'keystore.properties' (remove .example)
# 2. Fill in your actual keystore values
# 3. NEVER commit keystore.properties to version control
#
# To generate a new keystore, run:
# keytool -genkey -v -keystore turnoshospi-release.keystore -alias turnoshospi -keyalg RSA -keysize 2048 -validity 10000
#
# =============================================================================

# Path to your keystore file (relative to project root or absolute path)
storeFile=turnoshospi-release.keystore

# Password for the keystore
storePassword=YOUR_KEYSTORE_PASSWORD

# Alias of the key in the keystore
keyAlias=turnoshospi

# Password for the key
keyPassword=YOUR_KEY_PASSWORD
